# exclude SafeMath when calculating test coverage
# https://eth-brownie.readthedocs.io/en/v1.10.3/config.html#exclude_paths
reports:
  exclude_contracts:
    - SafeMath

dotenv: .env

networks:
  default: polygon-test

account-keys:
  private-key: ${ACCOUNT_PRIVATE_KEY} # ${} will get from .env file.
